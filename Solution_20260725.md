# 20260725 批次：表格高亮条目实现（Manus）

依据《苍白之王_杀塔机制整理20260725.xlsx》中高亮（未实现）条目，本批次改动如下。

## 一、新特质

1. **纯粹（Pure，名词 N#8）**
   - `Traits/CardTraits.cs`：新增 `IsPure(card)` / `ApplyPure(card)`；来源有两种——卡牌类自带（重写 `PaleRegentModV1Card.IsPure`）与战斗中附加（`ApplyPure`，灵魂护佑使用）。
   - 带纯粹的牌不会被【感染】（Infection 变形候选已排除纯粹牌）。
   - `static_hover_tips.json` 新增 `PURE` 词条。
2. **模具（Mould，名词 N#9）**
   - `PaleRegentModV1Card.IsMould`（虚拟属性）标记模具牌：国王佣卫、有翼佣卫、虚空化模。
   - `Traits/MouldHelper.cs`：消耗计数 + 战斗结束按「本场消耗数/100」概率发放「模具·［卡牌名］」遗物；已拥有则剩余场数 +1（升级/未升级按牌类型合并计数）。
   - `Patches/MouldExhaustListener.cs`：Harmony 补丁 `Hook.AfterCardExhausted` 计数、`CombatState.AddPlayer` 清零。
   - 结算入口挂在初始遗物 `PaleToken.AfterCombatEnd`（必存在且不重复）。
   - `static_hover_tips.json` 新增 `MOULD` 词条。

## 二、新遗物（模具体系）

- `Relics/MouldRelic.cs`（基类）+ `MouldKingsRetainer` / `MouldWingedRetainer` / `MouldVoidGivenMould`。
- 效果：你的每回合开始生成并打出对应牌（AutoPlay，期间 `MouldAutoPlayFlag=true`，HarnessPower 与有翼佣卫的 Harness 格挡加成被压制，即"去除 Harness 临时效果"）；1 场战斗后碎裂（`AfterCombatEnd` 剩余场数 -1，到 0 调 `RelicCmd.Remove`）。
- **已删除** `CastOffShellBlade` / `CastOffShellCarapace`（表格 R#6 备注要求），relics.json 同步清理。
- ⚠️ 待验证 API：`RelicCmd.Obtain(relic, player, 0)` 与 `RelicCmd.Remove(this)`（项目此前只用过 `RelicCmd.Replace`）。若编译/运行报错请回传日志。

## 三、新 Power

1. **旧日仇敌 AncientEnemyPower（P#5）**：集火标记独立成 Power（原先塞在 PlaguePower.FocusTarget 静态字段里）；PlaguePower 死亡转移时优先跳向带此标记的敌人。集火号令 FocusFireEdict 改为施加 1 层。
2. **失心诅咒 LostDestinyPower（P#20）**：持有者战斗中"生成"的苍白之王卡牌自动附加失心（入口在 `PaleRegentModV1Card.AfterCardGeneratedForCombat`，FailedVessel/PureVessel/VoidGivenFocus 的重写已补 `base` 调用）。
3. **虚空化模 VoidGivenMouldPower**：0 灵魂费卡牌伤害 +Amount（可叠加）。

## 四、新卡（7 张）

| 卡牌 | 类名 | 要点 |
|---|---|---|
| 黑暗潮汐 | DarkTide | 选择 1 张手牌附加失心；稀有度表格标"Ancient"，因未确认原版枚举暂用 Rare（备注） |
| 虚空化模 | VoidGivenMould | 技能，1 虚空费，消耗，模具；施加虚空化模 Power 5(7) |
| 异色 | OffColor | 攻击 5(7)；本回合每获得过 1 点虚空追打 1 次（VoidPowerListener 累计，PaleToken 回合开始清零） |
| 转生 | Transformation | 消耗；回复 3(4) HP，生成 1 张升级的弃壳入手 |
| 失心诅咒 | LostDestiny | 能力牌；获得失心诅咒 Power（升级费用 2→1） |
| 拥抱瘟疫 | Embrace | 随机自动打出 1(2) 张手牌攻击牌（AutoPlay） |
| 灵魂护佑 | SoulWard | 选 1 张手牌附加纯粹；抽 1 张牌（升级选 2 张） |

## 五、修复与调整（高亮文案/数值对齐）

- **PureSealPower BUG 修复**：`ModifyDamageMultiplicative` 只读不写（预览不再污染状态），实际封印移动改到 `AfterDamageGiven` 置位。
- **完美格挡 PerfectBlock**：删除自定义 EchoWardPower（回响守护，文件已删），改用原版覆甲 `PlatingPower`（3/5 层）。⚠️ 若编译找不到 `PlatingPower` 请回传报错。
- **驯化 Tame**：改为消耗所有牌堆中的虚空状态牌；新增失败实验分支。
- **疫刃 PlagueBlade**：按本场已消耗的感染数计伤，每张 +3(+5)。
- **壁垒誓言 BulwarkOath**：3(5) 层。
- **封印敕令 SealingEdict**：费用 1→2。
- **化神 ApotheosisPower**：升级后每回合选 2 张牌附加失心。
- **感染 Infection**：变形候选排除纯粹牌。
- **国王佣卫/有翼佣卫**：标记 IsMould；有翼佣卫在模具自动打出时跳过 Harness 格挡加成。
- cards.json / powers.json / relics.json / static_hover_tips.json 全部文案对齐表格。

## 六、遗留备注（需要你反馈/后续处理）

1. `RelicCmd.Obtain` / `RelicCmd.Remove` / `Hook.AfterCardExhausted` 三个 API 未在参考仓库找到用例，编译或运行如报错请回传日志。
2. DarkTide 稀有度：表格写 Ancient（先古），若原版 `CardRarity` 枚举含 Ancient 请告知，我改回。
3. 表格 S53/S54 要求"生成牌需注册 Hover Card Preview"——当前通过卡牌描述内的关键词提示实现基础展示，完整的 Hover Card Preview 注册 API 未在参考仓库找到，待有原版用例后补。
4. 新卡/新遗物图片暂缺（有默认占位 fallback，不会崩溃）：`dark_tide.png`、`void_given_mould.png`、`off_color.png`、`transformation.png`、`lost_destiny.png`、`embrace.png`、`soul_ward.png`；遗物 `mould_kings_retainer.png` 等。
